version: "3.9"

services:
  tensorflow:
    image: tensorflow/tensorflow:latest-gpu
    runtime: nvidia
    ports:
      - "8888:8888"
    volumes:
      - ./:/workspace
    stdin_open: true
    tty: true